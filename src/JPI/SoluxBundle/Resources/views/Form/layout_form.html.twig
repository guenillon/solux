{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_rest(form) }}

<div class="form-group">
	<div class="col-sm-2">
		<a href="{{ pathReturn }}" class="btn btn-default"><i class="glyphicon glyphicon-chevron-left"></i> {{ 'form.annuler'|trans }}</a>
	</div>
	<div class="col-sm-10">
		<button type="submit" class="btn btn-default pull-right"><span class="glyphicon glyphicon-ok"></span> {{ 'form.enregistrer'|trans }}</button>
	</div>
</div>
{{ form_end(form) }}
