{% extends "JPIUserBundle::layout.html.twig" %}

{% block fos_user_content %}
<div class="panel panel-default">
	<div class="panel-heading clearfix">
    	<div class="panel-title">
			<ul class="list-inline">
        		<li class="col-xs-12">
           			<h4 class="pull-left">Mon Compte</h4>
          			<span class="btn-group pull-right"> 
    					<a href="{{ path("fos_user_profile_edit") }}" class="btn btn-default"><span class="glyphicon glyphicon-edit"></span> Modifier</a>
    					<a href="{{ path("fos_user_change_password") }}" class="btn btn-default"><span class="glyphicon glyphicon-lock"></span> Mot de Passe</a>
       				</span>
				</li>
			</ul>
		</div>
  	</div>
	{% include "JPIUserBundle:Profile:show_content.html.twig" %}
</div>
{% endblock fos_user_content %}
