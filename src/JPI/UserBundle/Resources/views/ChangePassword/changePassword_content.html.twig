{{ form_start(form) }}
{{ form_rest(form) }}

<div class="form-group">
	<div class="col-sm-2">
		<a href="{{ path("fos_user_profile_show") }}" class="btn btn-default"><i class="glyphicon glyphicon-chevron-left"></i> Annuler</a>
	</div>
	<div class="col-sm-10">
		<button type="submit" class="btn btn-default pull-right"><span class="glyphicon glyphicon-ok"></span> {{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</button>
	</div>
</div>
{{ form_end(form) }}
